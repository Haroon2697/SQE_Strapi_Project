[pytest]
DJANGO_SETTINGS_MODULE = saleor.settings.test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=saleor
    --cov-report=xml
    --cov-report=html
    --cov-report=term-missing
    --reuse-db
    -v

